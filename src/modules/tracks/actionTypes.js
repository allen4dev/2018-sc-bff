import { NAME } from './constants';

export const ADD_TRACK = `${NAME}/ADD_TRACK`;
export const ADD_CREATED_TRACK = `${NAME}/ADD_CREATED_TRACK`;
export const ACTUALIZE_TRACK = `${NAME}/ACTUALIZE_TRACK`;
export const REMOVE_TRACK = `${NAME}/REMOVE_TRACK`;

export const ADD_TRACKS = `${NAME}/ADD_TRACKS`;

export const ADD_FAVORITED_TRACK = `${NAME}/ADD_FAVORITED_TRACK`;
