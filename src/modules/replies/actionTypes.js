import { NAME } from './constants';

export const ADD_TRACK_REPLY = `${NAME}/ADD_TRACK_REPLY`;
export const ADD_TRACK_REPLIES = `${NAME}/ADD_TRACK_REPLIES`;

export const ADD_REPLY_COMMENT = `${NAME}/ADD_REPLY_COMMENT`;
