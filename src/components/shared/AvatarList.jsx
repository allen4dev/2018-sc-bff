import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.ul``;

const AvatarList = () => (
  <Wrapper>
    <p>A AvatarList here</p>
  </Wrapper>
);

export default AvatarList;
